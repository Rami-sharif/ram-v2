# Rendered to secret.conf by scripts/render-thehive-config.sh (gitignored output).
# Play framework application secret, used to sign/encrypt sessions, cookies, and CSRF tokens.
# Placeholder substituted at render time with the real THEHIVE_SECRET value from .env;
# left untouched here — do not replace with a literal secret in this template file.
play.http.secret.key = "###THEHIVE_SECRET###"
